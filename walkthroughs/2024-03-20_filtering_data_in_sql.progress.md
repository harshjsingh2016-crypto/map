# Walkthrough — 2024-03-20_filtering_data_in_sql.md
Board: scalar-primers/sql-filtering
Source: Armor/Output/2024-03-20_filtering_data_in_sql.md
Updated: 2026-08-31
Agenda confirmed: 2026-08-30 (summary + glossary presented; §12 proposed as skip)
Learning complete: 2026-08-31 — every section discussed and ruled on; strip advanced to Class Assignment

Board was created fresh for this walkthrough — no prior coverage, so every item is **missed**.
Source is video-only: no class-notes PDF exists, so no second authority on identifier spelling.
Wiki articles from `scalar-primers/sql-intro` were read for continuity; none list this board yet.

- [x] §1 CONCAT — joining strings, separators as arguments, the semicolon rule — missed — discussed ✓ — accepted (String functions mindmap with the CONCAT branch — the container §2–§4 grow into; full_name query note with the SELECT *, reading and the customer/customers spelling caveat. Semicolon rule folded into detail rather than given a widget — the syntax-gotchas note is parked until §6, when it has company)
- [x] §2 UPPER / LOWER and function nesting — missed — discussed ✓ — accepted (two branches under String functions: case conversion, and functions-nest with the inner-resolves-first mechanism in detail. The new_name query appended to the existing CONCAT note rather than given its own widget — the two queries are one step apart and splitting them costs the comparison; note retitled)
- [x] §3 SUBSTR — (value, start, length), 1-indexing, the manual proper-case build, negative positions — missed — discussed ✓ — accepted (SUBSTR branch with three children: 1-indexing, the length-not-stop correction in amber — discharges the file caution in place with the SUBSTR('Shankar', 2, 6) evidence in detail — and omit-the-length-runs-to-the-end; proper-case-by-hand note with the no-space-argument point; negative-start table with the three worked expressions plus two amber rule rows. The last-n-characters pattern folded into the table as a row rather than given its own node)
- [x] §4 INITCAP — the BigQuery-only shortcut, and why the manual route was taught first — missed — discussed ✓ — accepted (INITCAP branch plus a BigQuery-only child in amber; docs-not-memorisation node under the root, where it covers the whole widget. The cross-board link to the intro class 90/10 rule carried as detail rather than a node — edges cannot cross boards — and made properly in the wiki, where sql-string-functions.md now relates to rdbms.md)
- [x] §5 the WHERE clause — the whiteboard table, Rule 1 (after FROM), Rule 2 (column must exist), data grain — missed — discussed ✓ — accepted (whiteboard table with the three P1 rows in green; two-rules note carrying the subset framing and the 90%-of-errors prediction; what-WHERE-can-and-cannot-see note on the revenue asymmetry, left deliberately open-ended pointing at §10. Data grain was to be folded into the table as detail — table rows carry no detail field, so it went into the two-rules note instead. Wiki: new article where-clause.md, the forward reference to order-of-execution left as prose, to become a link when §10 creates that article)
- [x] §6 choosing the operator — the =/!=/IN/NOT IN matrix, why OR not AND on one column, quoting rules — missed — discussed ✓ — accepted (four-way operator table with an amber one-column framing row; why-OR-not-AND note carrying the same-row reasoning and IN as the collapsed OR chain; syntax-gotchas note UNPARKED from §1 — quoting, case-sensitivity, !=/<>, and the semicolon rule MOVED here out of the CONCAT branch detail so the fact lives in one place. §8 will append no-IN-BETWEEN and no-spaces-in-a-date-literal; §11 will append == does not exist)
- [x] §7 worked examples on farmers_market — numeric comparison, range plus an extra value, two columns — missed — discussed ✓ — accepted (AND-or-OR table built on the can-one-row-satisfy-both test, which replaces the flat one-column-takes-OR reading; customer-4 note with the 1-of-50 page-size trap, FROM-before-SELECT, the low-confidence cost_to_customer_per_qty caveat and a one-line ORDER BY gloss at the user's request — sorting only, mechanics deferred; operator-precedence note in amber, labelled as the extraction's caution rather than the instructor's. The proposed flag on ORDER BY was replaced by that brief gloss. > is not inclusive appended to the syntax-gotchas note)
- [x] §8 BETWEEN — inclusive both ends, column named once, dates and the YYYY-MM-DD format — missed — discussed ✓ — accepted (BETWEEN note with both properties, the off-by-one on rewriting a > / < range, and the still-an-AND framing; BETWEEN-on-dates note with the vendor query and the vendor_booth_assignments caveat — the lowest-confidence identifier in the file. Refinements: syntax-gotchas note gained the date-literal rule and no-IN-BETWEEN, with the loud-vs-silent failure contrast; precedence note gained a closing line scoping the instructor's brackets-have-no-impact claim to where it is true, rather than leaving it implicitly contradicted; AND-or-OR table range row now names BETWEEN so the table stays the single answer to which-operator)
- [x] §9 IN and the case-sensitivity trick — LOWER() on both sides, and why it does not change the output — missed — discussed ✓ — accepted (mixed-case note recording the live failure — a well-formed result with a row silently missing — and the both-sides fix. Refinements: the §5 note gained the reverse direction (what WHERE computes never reaches the output) and was retitled "WHERE and SELECT — what each can see", making it the single place the boundary lives and where §10 will land; the gotchas case-sensitivity entry gained the normalise-both-sides fix so it is actionable on its own)
- [x] §10 order of execution — written order vs executed order, the kitchen analogy, why Rule 2 follows — missed — discussed ✓ — accepted (six-stage execution-order timeline with the kitchen step in each detail and amber markers on the two clauses that move; written-vs-executed note carrying both strings and the filter-before-sort argument; the payoff appended to the WHERE-and-SELECT note, which closes the thread deliberately left open at §5. Wiki: new article sql-order-of-execution.md, and where-clause.md's three forward references now resolve to it)
- [x] §11 quiz items — four, two of them low-confidence on the option letter — missed — discussed ✓ — accepted (quizzes note only, mirroring the sql-intro board's equivalent widget: the four teaching points, each pointing at the widget that already answers it, plus quiz 2's contradiction named as unreconcilable with the safe SUBSTR(name, -2) form beside it. The proposed == append to the syntax-gotchas note was declined — the fact lives in the quizzes note instead, one fact one place. Option letters deliberately not carried: two of four are contradicted in the recording, and a letter without its option text points at nothing even when correct)
- [x] §12 class assignment — eight problems, never discussed in the recording — missed — skipped by user at agenda confirmation (course admin; it is the next stage, not a concept)
- [x] §13 doubt-session answers — semicolons, INITCAP in MySQL, BigQuery "table not found", NULLs deferred — missed — discussed ✓ — accepted (only the two genuinely new items: the BigQuery table-not-found note, which doubles as the procedure that settles every transcript-derived identifier on this board — three widgets carry a verify-in-the-panel caveat pointing at it; and the NULLs note in amber, registering the hole rather than leaving it implicit. Six other doubt-session answers were already boarded at §1, §4, §5, §6 and §10 and were not duplicated. Course-context answer about the first four classes being light: proposed as skip, accepted)
- [x] Cautions — missed — discussed ✓ — all discharged in place, no separate widget: SUBSTR length-not-stop as an amber node (§3); operator precedence as its own amber note labelled as the extraction's caution, with the instructor's brackets claim scoped rather than left contradicted (§7–§8); unverified identifiers carried as caveats on the three widgets that use them, with the BigQuery panel procedure boarded as the resolution method (§13); ORDER BY/LIMIT/OFFSET used-but-not-taught glossed at §7 and placed on the execution timeline at §10; NULL deferral boarded as its own amber note (§13); garbled quiz option letters deliberately not carried (§11)
