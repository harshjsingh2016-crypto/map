node_modules/
dist/
*.log

# Boards and the wiki sync between machines via git (scripts/sync.mjs).
# PDF exports are regenerable, so they stay local.
pdfs/

# Machine-local Claude Code settings (launch.json is shared).
.claude/settings.local.json
